services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    env_file:
      - .env.production
    restart: unless-stopped
    # Prevent the container from using excessive resources.
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M
